## 概要

システムや環境の初期設定を行い、その結果（Output）を後続の多数のActivityへのControl（設定情報・制約）として配布するパターン。一度設定すれば多くの活動に適用できる。

## 使いどころ

- 環境設定（開発・本番の設定値）
- ガバナンスポリシーの一元管理と適用
- マスタデータの定義と配布

## 効果

設定の変更が一箇所（このActivity）で完結し、後続の全Activityに一貫して反映される。設定のドリフト（環境間の不一致）を防ぐ。
